services:
  - type: web
    name: bici
    env: node
    autoDeploy: true
    buildCommand: npm ci && npm run build
    startCommand: node node_modules/next/dist/bin/next start -p $PORT -H 0.0.0.0
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
